image:
  name: hashicorp/terraform:1.14.8
  entrypoint: [""]

stages:
  - init
  - validate
  - plan
  - apply
  - ansible
  - destroy
variables:
  TF_ROOT: "."
  # DEFAULT ENV se CI_ENVIRONMENT_NAME estiver vazio
  ENV: "${CI_ENVIRONMENT_NAME}"
  # fallback para dev se ENV estiver vazio (GitLab não expande ${VAR:-default} em fields)
  ENV_DEFAULT: "hml"
  # Decide qual usar: se ENV vazio, usa ENV_DEFAULT
  ENV_RESOLVED: "${ENV}"
  TF_STATE_NAME: "${ENV_RESOLVED}"
  TF_ADDRESS: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/terraform/state/${TF_STATE_NAME}"

  TF_HTTP_ADDRESS: "${TF_ADDRESS}"
  TF_HTTP_LOCK_ADDRESS: "${TF_ADDRESS}/lock"
  TF_HTTP_UNLOCK_ADDRESS: "${TF_ADDRESS}/lock"
  TF_HTTP_LOCK_METHOD: "POST"
  TF_HTTP_UNLOCK_METHOD: "DELETE"
  TF_HTTP_USERNAME: "gitlab-ci-token"
  TF_HTTP_PASSWORD: "${CI_JOB_TOKEN}"
  TF_IN_AUTOMATION: "true"
  VCD_CACHE: "true" # Ativa o mecanismo de cache [1], [2]

before_script:
  - cd ${TF_ROOT}
  # Resolve ENV se estiver vazio (shell expansion aqui funciona)
  - |
    if [ -z "${ENV}" ]; then
      export ENV="${ENV_DEFAULT}"
    fi
    export TF_STATE_NAME="${ENV}"
    export TF_HTTP_ADDRESS="${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/terraform/state/${TF_STATE_NAME}"
    export TF_HTTP_LOCK_ADDRESS="${TF_HTTP_ADDRESS}/lock"
    export TF_HTTP_UNLOCK_ADDRESS="${TF_HTTP_ADDRESS}/lock"
    export TF_HTTP_USERNAME="gitlab-ci-token"
    export TF_HTTP_PASSWORD="${CI_JOB_TOKEN}"
  - terraform --version

init:
  stage: init
  script:
    - terraform init -input=false -reconfigure

validate:
  stage: validate
  script:
    - terraform init -input=false -reconfigure
    - terraform fmt -check || true
    - terraform validate

plan:
  stage: plan
  environment:
    name: "${ENV_DEFAULT}"
  script:
    - echo "DEBUG VARS:"
    - env | grep TF_VAR
    - echo "ENV=${ENV_DEFAULT}"
    - ls -la environments || true
    - test -f "environments/${ENV}.tfvars" || (echo "Arquivo environments/${ENV}.tfvars não encontrado" && exit 1)
    - terraform init -input=false -reconfigure
    - terraform plan -input=false -out=tfplan  -var="vcd_api_token=${TF_VAR_vcd_api_token}" -var-file="environments/${ENV}.tfvars"
  artifacts:
    paths:
      - tfplan
    expire_in: 2h 

apply:
  stage: apply
  environment:
    name: "${ENV}"
  dependencies:
    - plan
  when: manual
  script:
    - terraform init -input=false -reconfigure 
    - |
      if terraform apply -input=false tfplan; then
        echo "Apply ok"
      else
        echo "Plan stale ou erro — recriando plan e aplicando"
        terraform plan -input=false -out=tfplan -var-file="environments/${ENV}.tfvars"
        terraform apply -input=false tfplan
      fi

ansible-provision:
  stage: ansible
  image: alpine/curl
  needs:
    - terraform-apply
  when: manual
  script:
    - apk add --no-cache jq
    - VM_IPS=$(cat vm_ips.json)
    - >-
      JOB_ID=$(curl -s -X POST
      https://awx.seudominio.com/api/v2/job_templates/${AWX_TEMPLATE_ID}/launch/
      -H "Authorization: Bearer ${AWX_TOKEN}"
      -H "Content-Type: application/json"
      -d "{\"extra_vars\": {\"vm_ips\": $VM_IPS}}"
      | jq -r '.id')
    - echo "AWX Job ID $JOB_ID"
    - |
      while true; do
        STATUS=$(curl -s \
          https://awx.seudominio.com/api/v2/jobs/${JOB_ID}/ \
          -H "Authorization: Bearer ${AWX_TOKEN}" \
          | jq -r '.status')
        echo "Status $STATUS"
        if [ "$STATUS" = "successful" ]; then
          echo "Job concluido com sucesso"
          break
        elif [ "$STATUS" = "failed" ] || [ "$STATUS" = "error" ] || [ "$STATUS" = "canceled" ]; then
          echo "Job falhou com status $STATUS"
          exit 1
        fi
        sleep 15
      done
  variables:
    AWX_TEMPLATE_ID: "42"

destroy:
  stage: destroy
  environment:
    name: "${ENV}"
    action: stop
  when: manual
  script:
    - terraform init -input=false -reconfigure
    - terraform destroy -auto-approve -var="vcd_api_token=${TF_VAR_vcd_api_token}" -var-file="environments/${ENV}.tfvars"